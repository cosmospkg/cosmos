[package]
name = "cosmos-universe"
version = "1.0.1"
edition = "2021"
publish = false

[lib]
crate-type = ['rlib']

[dependencies]
semver = { version = "1.0.26", features = ["serde"] }
serde = { version = "1.0.219", features = ["derive"] }
toml = "0.8.20"
